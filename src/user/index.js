
const user = {
    getUsers: (accountId) => {
        let result = {
            message: 'error getting users for account'
        }

        return result
    },
    getUser: (email) => {
        let result = {
            message: 'error getting user for id'
        }

        return result
    },
    // createUser: (data) => {
    //     let result = {
    //         message: 'error creating user'
    //     }

    //     return result
    // }
}

export default user